{
  "description": "do loop hook for 7-phase workflow",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ \"$OS\" = \"Windows_NT\" ] && cmd.exe /c \"${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.bat\" || \"${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.sh\""
          }
        ]
      }
    ]
  }
}
